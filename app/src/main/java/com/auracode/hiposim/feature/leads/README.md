# feature/leads

Reserved for the real estate connection (US21): sending the quote to agencies and tracking leads.
Nothing is implemented yet. When it is, it follows the same `ui` / `domain` / `data` split as the other features.
