# feature/auth/data

Empty on purpose: there is no account backend yet. The `AuthRepository` interface goes in `domain`
and its API-backed implementation here when real registration and sign in are built.
