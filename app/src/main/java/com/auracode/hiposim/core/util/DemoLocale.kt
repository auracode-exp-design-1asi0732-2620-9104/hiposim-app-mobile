package com.auracode.hiposim.core.util

/**
 * Initial UI language of the demo, as a BCP 47 tag.
 *
 * The course brief asks for English by default (`res/values`). The demo is presented in Spanish
 * (`res/values-b+es+419`), so MainActivity forces this tag. Set it to `"en"` to follow the brief,
 * or to an empty string to follow the device language.
 */
const val DEMO_LANGUAGE_TAG: String = "es-419"
